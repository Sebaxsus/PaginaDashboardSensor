<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Dashboard Sensor</title>
	</head>
	<body class="Page-Container">
		<slot />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		background-color: light-dark(beige, black);

		color-scheme: light dark;
		color: light-dark(black, beige);

	}
	.Page-Container {
		position: relative;

		display: grid;
		grid-template-columns: minmax(100px, 0.3fr) minmax(300px, 1fr);
		grid-template-rows: minmax(500px, 1fr) minmax(100px, auto);
		grid-template-areas: 
			"aside main main"
			"player player player"
		;
		gap: 8px;

		padding: 4px;
	}
	
</style>
