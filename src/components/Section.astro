---
// Seccion JS y Imports
interface Props {
    styling?: string;
}

const { styling } = Astro.props
---

<section class={`Page-Section ${styling}`}>
    <slot />
</section>

<style>
    .Page-Section {
        border-radius: .5rem;
        background-color: #2b2b2b;

        padding: 4px 6px;

        display: flex;
        gap: 4px;
    }
</style>